{
  "name": "asksoe",
  "version": "1.0.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "tsx server/index.ts",
    "build": "npm run build:client && npm run build:server",
    "build:client": "cd client && npm install && npm run build",
    "build:server": "cd server && npm install && npm run build",
    "build:railway": "cd server && npm install && npm run build",
    "build:vercel": "cd client && cp ../shared/schema.ts ./src/shared/ && npm install && npm run build",
    "start": "NODE_ENV=production node dist/index.js",
    "start:railway": "cd server && npm run start",
    "check": "tsc",
    "db:push": "drizzle-kit push"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
